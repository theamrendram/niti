import Navbar from "@/components/navbar";
import HeroSection from "@/components/hero-section";
const Home = () => {
  return (
    <div>
      <Navbar/>
      <HeroSection/>
    </div>
  );
};

export default Home;
